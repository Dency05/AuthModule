server:
  port: 7001
spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/auth

  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG